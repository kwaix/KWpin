{
  "name": "pinball-sample",
  "version": "0.1.0",
  "description": "Firestore 리더보드 + Gemini API + GitHub Actions용 핀볼 게임 샘플 프로젝트",
  "type": "module",
  "scripts": {
    "test": "jest",
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --outfile=build/main.js --format=esm --target=es2020"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "esbuild": "^0.27.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "firebase": "^12.6.0"
  }
}
